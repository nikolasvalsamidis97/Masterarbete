# Validation Scripts

Validation and regression projects for checking isolated parts of the photon-pressure workflow.

Each subfolder is a validation project with its own `README.md`, script, and ignored local `results/` folder.
