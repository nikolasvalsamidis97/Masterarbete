# Validation Molecule Convergence

Checks molecule beta convergence against column-density and grid choices.

## Scripts

- `molecule_conv.py`: main convergence validation script.

## Run

From the repository root:

```bash
python "Projects/Validation Scripts/Validation_Molecule_Convergence/molecule_conv.py"
```

Generated validation outputs are written under `results/`, which is ignored by Git.
