# Validation Strength Limit

Checks cutoff and strength-limit behavior for molecule line handling.

## Scripts

- `strength_limit_validation.py`: main validation script.

## Run

From the repository root:

```bash
python "Projects/Validation Scripts/Validation_Strength_Limit/strength_limit_validation.py"
```

Generated validation outputs are written under `results/`, which is ignored by Git.
